from flask import Flask, render_template, request
from EmotionDetection.emotion_detection import emotion_detector

app = Flask("Emotion Detector")

@app.route("/")
def home_route():
    return render_template("index.html")

@app.route("/emotionDetector")
def emotion_analyzer():
    text_to_analyze = request.args.get("textToAnalyze")
    analyzed_text = emotion_detector(text_to_analyze)
    result = f"For the given statement, the system response is 'anger': {analyzed_text.get('anger')}, 'disgust': {analyzed_text.get('disgust')}, 'fear': {analyzed_text.get('fear')}, 'joy': {analyzed_text.get('joy')} and 'sadness': {analyzed_text.get('sadness')}. The dominant emotion is {analyzed_text.get('dominant_emotion')}."
    return result

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)